const cfg=window.HUB_ACCEPTANCE_CONFIG,enc=b=>btoa(String.fromCharCode(...new Uint8Array(b))).replace(/\+/g,"-").replace(/\//g,"_").replace(/=+$/,""),random=n=>{const b=new Uint8Array(n);crypto.getRandomValues(b);return enc(b)};
document.querySelector("#connect").onclick=async()=>{try{const clientId=cfg.oauthClientId;if(!clientId)throw new Error("Acceptance Client ID не настроен");const verifier=random(64),state=random(32),challenge=enc(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(verifier)));sessionStorage.setItem("hub_acceptance_oauth_verifier",verifier);sessionStorage.setItem("hub_acceptance_oauth_state",state);sessionStorage.setItem("hub_acceptance_oauth_client_id",clientId);sessionStorage.removeItem("hub_acceptance_access_token");const q=new URLSearchParams({response_type:"code",client_id:clientId,redirect_uri:cfg.redirectUri,state,code_challenge:challenge,code_challenge_method:"S256",scope:"email"});location.assign(cfg.supabaseUrl+"/auth/v1/oauth/authorize?"+q)}catch(e){document.querySelector("#error").textContent=e.message}};
