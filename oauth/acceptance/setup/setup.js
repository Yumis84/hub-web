import{createClient}from"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm";const c=window.HUB_ACCEPTANCE_CONFIG,sb=createClient(c.supabaseUrl,c.supabasePublishableKey),o=document.querySelector("#out");document.querySelector("#f").onsubmit=async e=>{e.preventDefault();o.textContent="Создание…";const email=document.querySelector("#email").value.trim(),password=document.querySelector("#password").value;const{data,error}=await sb.auth.signUp({email,password});if(error){o.textContent="Ошибка: "+error.message;return}o.textContent=data.session?"PASS — пользователь создан и сессия активна. Вернитесь в OAuth test-client.":"Пользователь создан. Если проект требует подтверждение email, подтвердите письмо перед OAuth-входом.";};